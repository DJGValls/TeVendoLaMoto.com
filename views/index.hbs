    <div class="d-flex flex-row justify-content-start flex-wrap pl-100">
    {{#each allProduct}}
        <div class="card shadow-lg mt-5 mr-5 bg-white rounded justify-content-evenly" style="width: 18rem;">
            <a href="/product/{{this._id}}/details" class="text-decoration-none">
                <div class="card-body bg-gradient bg-opacity-75">
                    <img class="card-img-top" src={{this.img}} alt="img" width="300px">
                    <div class="d-flex flex-column mt-3 align-items-center text-dark">
                        <h3 class="mb-4">{{this.nombre}}</h3>
                        <h5 class="mb-4">Precio: {{this.precio}}€</h2>
                        <p class=" mb-3">{{this.descripcion}}</p>
                        <p>{{this.vendedor.username}}</p>
                    </div>
                </div>   
            </a>
        </div>
    {{/each}}
    </div>