<div class="d-flex flex-row mt-3">
  {{!-- contenedor mensajes --}}
  <div class ="d-flex flex-column justify-content-start flex-wrap ml-3">
  {{#each todosLosMensajes}}
    <div class=" card shadow-lg mt-3 p-3 pb-0 bg-white rounded" style="width: 16rem;">
          <h2 class="mb-4">{{this.producto.nombre}}</h2>
          <h4>Puja:{{this.mensaje}}€</h4>
          <h5>{{this.estadoPuja}}</h5>
          <div class="d-flex flex-row justify-content-center justify-content-evenly mt-3">
            <form action="/user/{{this._id}}/update" method="post">
            <button class="btn btn-success">Aceptar</button>
            </form> 
            <form action="/user/{{this._id}}/delete" method="post">
            <button class="btn btn-danger">Rechazar</button>
            </form>
          </div>
    </div>
    {{/each}}
  </div>
{{!-- contenedor prductos --}}
  <div class="d-flex flex-row justify-content-start flex-wrap ml-5">
    {{#each allProduct}}
    <div class="card shadow-lg p-1 mt-3 mr-5  bg-white rounded" style="width: 20rem;">
      <img class="card-img-top" src= {{this.img}} alt="img" height="200rem">
        <div class="card-body pb-0 bg-gradient bg-opacity-75">
          <div class="d-flex flex-column align-items-center">
            <h3>{{this.nombre}}</h3>
            <h4>{{this.precio}}€</h4>
            <p>{{this.descripcion}}</p>
          </div>
          <div class="d-flex flex-row justify-content-evenly align-content-end">
            <a href="/product/{{this._id}}/edit">
            <button class="btn btn-info">Editar</button>
            </a>
            <form action="/product/{{this._id}}/delete" method="post">
              <button class="btn btn-danger">Borrar</button>
            </form>
          </div>
        </div> 
    </div> 
      {{/each}}
  </div>
  
    <div class="d-flex flex-column mr-7">
        <a  class=" boton-añadir" href="/product/create-product">
        <img src="/images/Green-Add-Button-PNG-HD.png" alt="mas" width="100px">
        </a>
   </div>
</div>



  
